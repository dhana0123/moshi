# Moshi Inner Monologue packer deps (same idea as pyproject [data] extra).
#
# IMPORTANT: whisperx / pyannote may try to replace your CUDA torch.
# Prefer:
#   pip install -e ".[data]" --upgrade-strategy only-if-needed
#
# If torch/torchaudio were uninstalled, RESTORE them after (example cu128):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# Do NOT list torch/torchaudio here — keep the wheels you already installed.
#
# Gated HF cards (same account as HF_TOKEN):
#   https://huggingface.co/datasets/ai4bharat/IndicVoices
#   https://huggingface.co/pyannote/speaker-diarization-3.1
#   https://huggingface.co/pyannote/segmentation-3.0
# Prefer pyannote.audio 3.x (<4) so community-1 is not required.

datasets>=2.19
huggingface-hub>=0.24
soundfile>=0.12
scipy>=1.11
pandas>=2.0
pyannote.audio>=3.1.0,<4.0
nemo_toolkit[asr]
omegaconf
hydra-core
whisperx
transformers>=4.40
nltk
openai-whisper
